export class AssetManager {
    meshes = new Map();
    sounds = new Map();

    constructor(scene) {
        this.scene = scene;
    }

    async loadFightAssets() {
        // TODO Romain load fight assets
    }
}